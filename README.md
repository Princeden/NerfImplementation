### Simple NeRF Implementation

Implementation of the NERF model using PyTorch. The original paper can be found here:

#### Explanation
The paper does a very good job explaining the thought process behind the model but I will try and break it down simpler. 

The premise of the model is training a MLP to take a position and view angle then translate that position into a color to render. 


